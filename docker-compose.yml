version: "3.9"
services:
  db:
    image: postgres:15.2
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
  backend:
    build: ./backend
    user: "${UID:?Set UID env variable to your user id}"
    depends_on:
      - db

volumes:
  dbdata:
