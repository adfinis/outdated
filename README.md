# Outdated

An application to track the dependencies of projects.

## Requirements

- Git
- Node.js
- yarn
- docker compose

## Running

To run the entire application:

- `echo "UID=$(id -u)" > .env`
- `make start`
