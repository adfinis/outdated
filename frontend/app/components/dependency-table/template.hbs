{{#if @dependencyVersions}}
  <table class='uk-height-small'>
    <thead>
      <tr>
        <th>Dependency</th>
        <th>Version</th>
        <th>End Of Life Date</th>
        <th>Release Date</th>
      </tr>
    </thead>
    <tbody>
      {{#each (sort-by 'eolDate:asc' @dependencyVersions) as |version|}}
        <DependencyDetailed @version={{version}} />
      {{/each}}

    </tbody>
  </table>
{{else}}
  <div class='none-yet' data-test-dependency-versions-none>
    <span class='text-UNDEFINED'>No dependencies yet</span>
  </div>
{{/if}}